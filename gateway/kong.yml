_format_version: "2.1"
_transform: true

services:
- name: auth-service
  url: http://localhost:5000
  routes:
  - name: auth-route
    paths:
    - /auth
- name: bookdet-reviews-service
  url: http://localhost:5001
  routes:
  - name: bookdet-reviews-get
    paths:
     - /bookdetails